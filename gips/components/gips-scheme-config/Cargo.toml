[package]
name = "gips-scheme-config"
version = "0.1.0"
edition = "2021"

[dependencies]
gips-config = { path = "../../bases/gips-config" }
gips-trust = { path = "../gips-trust" }
anyhow = { workspace = true }
toml = "0.8"
tokio = { workspace = true }

[dev-dependencies]
tempfile = "3"
