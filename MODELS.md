# Pipeline Agent Configuration

This file supplies the stable agent-name lookup required by the staged
pipeline. Capability and model-routing policy remains in `MODEL.md`.

## Pipeline agents

| role | agent | notes |
|---|---|---|
| executor | stage-executor | Implements one committed stage in an isolated worktree. |
